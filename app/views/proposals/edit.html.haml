%h1= t('.edit_proposaltitle_for_event', :proposaltitle => (proposal.title), :event => (event))

= simple_form_for proposal, url: proposal_path(event.slug, proposal) do |f|
  = render partial: 'form', locals: {f: f}
