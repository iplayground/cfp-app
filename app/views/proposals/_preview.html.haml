#proposal-preview.panel.panel-default{ data: { 'remote-url' => parse_edit_field_proposal_path(event.slug) } }
  .panel-heading
    %h2.panel-title= t('.preview')
  .panel-body
    = render partial: 'proposals/contents', locals: { proposal: proposal }
